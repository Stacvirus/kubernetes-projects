apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: todoapp-claim
  namespace: project
spec:
  storageClassName: todo-pv-class
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi