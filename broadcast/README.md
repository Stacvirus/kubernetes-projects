### Building a Go http service as Nats server consumer

# Initialize a new Go module:
`go mod init event-driven-app`

# First, we need to install the NATS Go client library:
`go get github.com/nats-io/nats.go`
