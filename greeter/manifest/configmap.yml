apiVersion: v1
kind: ConfigMap
metadata:
  name: hash-config
data:
  MESSAGE: "hello world"
  PONG_SERVICE_URL: "http://ping-pong-svc:2453/pings"
  GREETER_SERVICE_URL: "http://greeter-svc:2020/greet"
  
  information.txt: |
    this text is from file

---
apiVersion: v1
kind: ConfigMap
metadata:
  name: db-init
data:
  init.sql: |
    CREATE TABLE IF NOT EXISTS lines (
        id SERIAL PRIMARY KEY,
        content TEXT NOT NULL,
        created_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP
    );